{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The connect page: the IPC it needs is the commands the shell defines, plus opening a sign-in in the system browser and being sent back to it.",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:default",
    "opener:allow-open-url",
    "deep-link:default"
  ]
}
