{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The connect page: the IPC it needs is the two commands the shell defines.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
