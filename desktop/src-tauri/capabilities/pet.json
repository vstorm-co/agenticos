{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "pet",
  "description": "The pet moves its own window: strolls set its position, a press starts a native drag.",
  "windows": ["pet"],
  "permissions": ["core:default", "core:window:allow-start-dragging", "core:window:allow-set-position"]
}
