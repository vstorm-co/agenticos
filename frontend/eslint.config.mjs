import nextCoreWebVitals from "eslint-config-next/core-web-vitals";
import nextTypescript from "eslint-config-next/typescript";
import jsxA11y from "eslint-plugin-jsx-a11y";

// Imported directly rather than through `FlatCompat`. eslint-config-next 16
// ships flat config natively, and pushing it back through the eslintrc
// compatibility layer makes the plugin graph self-referential - ESLint dies
// serializing it, with "Converting circular structure to JSON" and nothing
// naming the config responsible.
const eslintConfig = [
  ...nextCoreWebVitals,
  ...nextTypescript,
  {
    ignores: [
      // Generated by vitest; not source.
      "coverage/**",
      ".next/",
      "node_modules/",
      "out/",
      "next-env.d.ts",
    ],
  },
  // Accessibility: enforce the jsx-a11y recommended ruleset explicitly.
  // next/core-web-vitals already registers the "jsx-a11y" plugin but only
  // enables a small subset of its rules, so we re-use that plugin registration
  // and turn on the full recommended ruleset.
  {
    rules: {
      ...jsxA11y.flatConfigs.recommended.rules,
      // High-value rules — keep as errors so they block the build.
      "jsx-a11y/alt-text": "error",
      "jsx-a11y/label-has-associated-control": [
        "error",
        {
          // Treat these custom/Radix form controls as valid label targets so
          // labels that wrap them aren't flagged as orphaned.
          controlComponents: ["Checkbox", "Switch", "RadioGroupItem", "Slider"],
          assert: "either",
        },
      ],
      "jsx-a11y/aria-props": "error",
      "jsx-a11y/aria-proptypes": "error",
      "jsx-a11y/aria-unsupported-elements": "error",
      "jsx-a11y/role-has-required-aria-props": "error",
      "jsx-a11y/role-supports-aria-props": "error",
      "jsx-a11y/anchor-has-content": "error",
      "jsx-a11y/heading-has-content": "error",
      // Noisier / structural rules — surface as warnings so they're visible
      // without blocking the build. These typically require larger refactors
      // (keyboard handlers, captions, native-element swaps) to satisfy.
      "jsx-a11y/no-autofocus": "warn",
      "jsx-a11y/click-events-have-key-events": "warn",
      "jsx-a11y/no-static-element-interactions": "warn",
      "jsx-a11y/no-noninteractive-element-interactions": "warn",
      "jsx-a11y/media-has-caption": "warn",
    },
  },
  {
    // Accessibility and hook-dependency findings in template-inherited
    // components. They are real, and fixing them is a separate piece of work in
    // code we did not design. Scoped to these files so the same finding in our
    // own code still fails the build — this list shrinking is the progress
    // marker, exactly as with the backend's per-file ruff ignores.
    files: [
      "src/app/**/kb/**",
      "src/components/chat/**",
      "src/components/rag/**",
      "src/components/ui/**",
      "src/components/dashboard/**",
      "src/components/settings/**",
      "src/components/onboarding/**",
      "src/components/kb/**",
      "src/components/teams/**",
      "src/components/layout/command-palette.tsx",
      "src/app/**/admin/**",
      "src/app/**/orgs/**",
    ],
    rules: {
      "jsx-a11y/no-autofocus": "off",
      "jsx-a11y/click-events-have-key-events": "off",
      "jsx-a11y/no-static-element-interactions": "off",
      "jsx-a11y/no-noninteractive-element-interactions": "off",
      "jsx-a11y/media-has-caption": "off",
      "react-hooks/exhaustive-deps": "off",
    },
  },
  {
    rules: {
      "@typescript-eslint/no-unused-vars": [
        "error",
        {
          argsIgnorePattern: "^_",
          varsIgnorePattern: "^_",
          caughtErrorsIgnorePattern: "^_",
          destructuredArrayIgnorePattern: "^_",
        },
      ],
    },
  },
];

export default eslintConfig;
